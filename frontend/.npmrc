# 跳过 pnpm 运行 script 前的依赖一致性校验。
# pnpm 11 默认会在 `pnpm <script>` 之前自动跑一次 install，
# 触发 ERR_PNPM_IGNORED_BUILDS 警告并以非零码退出，
# 中断 run_linux.sh 的 set -e 控制流。
# 我们在 run_linux.sh 中已显式调用 install + build，所以可以关闭这个自动校验。
verify-deps-before-run=never
